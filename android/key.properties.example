# Keystore configuration for signing release builds
# Duplicate this file as 'key.properties' in this directory and fill in your actual credentials.
# NEVER commit key.properties or the .jks/.keystore file to version control!

storePassword=your_keystore_password
keyPassword=your_key_password
keyAlias=upload
storeFile=../upload-keystore.jks
